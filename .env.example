# Environment Configuration Template
# This file shows all available environment variables
# 
# Environment files are loaded in this order (later ones override earlier):
# 1. .env                    - Defaults for all environments
# 2. .env.development        - Development-specific (npm run dev)
# 3. .env.production         - Production-specific (npm run build)
# 4. .env.local              - Local overrides (gitignored, highest priority)

# ============================================
# API Configuration
# ============================================
# API Base URL (without trailing slash)
# AWS API Gateway endpoint
VITE_API_BASE_URL=https://yourURL.execute-api.us-east-1.amazonaws.com

# For local backend testing, use .env.local with:
# VITE_API_BASE_URL=http://localhost:5000

# ============================================
# AWS Cognito Authentication
# ============================================
# User Pool ID (format: us-east-1_XXXXXXXXX)
VITE_COGNITO_USER_POOL_ID=your-user-pool-id

# App Client ID
VITE_COGNITO_CLIENT_ID=your-client-id

# Cognito Domain (format: your-domain.auth.region.amazoncognito.com)
VITE_COGNITO_DOMAIN=your-cognito-domain.auth.us-east-1.amazoncognito.com

# AWS Region
VITE_COGNITO_REGION=us-east-1

# ============================================
# Auth Configuration (Optional)
# ============================================
# Set to 'true' to enable mock auth mode (no token required)
# VITE_MOCK_AUTH=false

# ============================================
# Feature Flags (Optional)
# ============================================
# VITE_ENABLE_DIAGRAM_EXPORT=true
# VITE_ENABLE_DIAGRAM_SHARE=false

# ============================================
# Application Settings (Optional)
# ============================================
# VITE_APP_NAME=UML Editor
# VITE_MAX_DIAGRAM_SIZE=1048576
